{
  "name": "json-preserve-indent",
  "description": "read from, write to and return a JSON string, minimizing diffs and preserving indents/fileending",
  "main": "index.js",
  "scripts": {
    "test": "standard && nyc tap --no-cov test.js",
    "test:coverage": "nyc report --reporter=text-lcov | coveralls",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/boennemann/json-preserve-indent.git"
  },
  "keywords": [
    "json",
    "formatting",
    "read",
    "write"
  ],
  "author": "Stephan Bönnemann <stephan@boennemann.me> (http://boennemann.me/)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/boennemann/json-preserve-indent/issues"
  },
  "homepage": "https://github.com/boennemann/json-preserve-indent#readme",
  "dependencies": {
    "lodash": "^3.10.1"
  },
  "devDependencies": {
    "coveralls": "^2.11.4",
    "nyc": "^5.0.0",
    "standard": "^5.3.1",
    "tap": "^2.1.1",
    "semantic-release": "^4.3.5"
  }
}
